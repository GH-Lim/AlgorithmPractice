from collections import deque
from itertools import permutations
def solution(n, weak, dist):
    answer = 0
    for p_dist in permutations(dist, len(dist)):
        cnt = 0
        while p_dist[cnt]



    return answer